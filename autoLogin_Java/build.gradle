// gradle clean build run
/*
 * This build file was auto generated by running the Gradle 'init' task
 * by 'jimmy' at '1/3/14 11:35 PM' with Gradle 1.10
 *
 * This generated file contains a commented-out sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at http://gradle.org/docs/1.10/userguide/tutorial_java_projects.html
 */

// Apply the java plugin to add support for Java
apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin:'application'

// In this section you declare where to find the dependencies of your project
repositories {
    // Use 'maven central' for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    mavenCentral()
}

task wrapper(type: Wrapper) {
	gradleVersion = '1.12'
}

// In this section you declare the dependencies for your production and test code
dependencies {
    runtime fileTree(dir: 'lib', include: '*.jar')
    
    // The production code uses the Log4J logging API at compile time
    compile fileTree(dir: 'lib', include: '*.jar')
    //compile files('lib/selenium-java-2.39.0.jar')
	
	compile group: 'org.seleniumhq.selenium', name: 'selenium-java', version: '2.41+'
    compile group: 'commons-logging', name: 'commons-logging', version: '1.1.1'
    compile group: 'log4j', name: 'log4j', version: '1.2.16'

    // Declare the dependency for your favourite test framework you want to use in your tests.
    // TestNG is also supported by the Gradle Test task. Just change the
    // testCompile dependency to testCompile 'org.testng:testng:6.8.1' and add
    // 'test.useTestNG()' to your build script.
    compile "junit:junit:4.11"
}

// Default Source Sets: src/main/java/packageName
/*
sourceSets {
    main {
        java {
            srcDir 'src/java'
        }
        resources {
            srcDir 'src/resources'
        }
    }
}
*/

mainClassName = "net.jimmyliao.selenium.HelloWebDriver"
// gradle clean
// gradle build
// gradle run

test {
    // Default test task scan src/test/*, need to replace
	// For Building Jar, excludes all unit tests
    //exclude '**'
    testClassesDir = sourceSets.main.output.classesDir
    //include 'com/ibm/ftm/cps/gui/login/LOGIN_01.class'    
}